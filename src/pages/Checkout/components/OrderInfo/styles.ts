import styled from 'styled-components'
import { FormSectionStyle } from '../../styles'

export const OrderInfoContainer = styled.div`
  width: 40rem;
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
`
export const FormSectionContainer = styled(FormSectionStyle)`
  display: flex;
  flex-direction: column;
  gap: 2rem;
`
